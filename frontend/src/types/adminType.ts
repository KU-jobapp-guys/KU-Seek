export interface User {
  userId: string
  name: string
  type: string
  document: string
  reason: string
  status: string
}

export interface Job {
  jobId: string
  title: string
  company: string
  status: string
  reason: string  
}